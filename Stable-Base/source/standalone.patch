diff --git a/CMakeLists.txt b/CMakeLists.txt
index 7e8079d..1722d63 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -2,9 +2,9 @@
 # https://github.com/twhl-community/halflife-unified-sdk
 cmake_minimum_required( VERSION 3.24 )
 
-if (WIN32)
+if (WIN32 AND CMAKE_GENERATOR MATCHES "Visual Studio")
 	set(CMAKE_GENERATOR_PLATFORM Win32)
-endif(WIN32)
+endif()
 
 if (MSVC)
 	set(CMAKE_SYSTEM_VERSION 10.0)
@@ -32,6 +32,11 @@ project(msr
 	VERSION 1.0.0
 	LANGUAGES CXX)
 
+option(MSR_STANDALONE "Build for Xash3D with private FN identities and no Steam integration" OFF)
+if(MSR_STANDALONE)
+	add_compile_definitions(MSR_STANDALONE)
+endif()
+
 set(CMAKE_CONFIGURATION_TYPES "Debug;Release" CACHE STRING "" FORCE)
 
 # Disable the use of absolute paths in library paths even in development builds.
diff --git a/src/game/client/CMakeLists.txt b/src/game/client/CMakeLists.txt
index f0a5b25..7200a7a 100644
--- a/src/game/client/CMakeLists.txt
+++ b/src/game/client/CMakeLists.txt
@@ -52,7 +52,7 @@ target_link_libraries(client PRIVATE
 	SDL2
 	fmod
 	discord
-	steam_api
+	$<$<NOT:$<BOOL:${MSR_STANDALONE}>>:steam_api>
 	${OPENGL_LIBRARIES})
 
 target_include_directories(client PRIVATE
diff --git a/src/game/client/SteamClientHelper.cpp b/src/game/client/SteamClientHelper.cpp
index 80c0073..2b51b7c 100644
--- a/src/game/client/SteamClientHelper.cpp
+++ b/src/game/client/SteamClientHelper.cpp
@@ -3,6 +3,15 @@
 #include "hud.h"
 #include "cl_util.h"
 
+#ifdef MSR_STANDALONE
+static CSteamClientHelper s_SteamHelper;
+CSteamClientHelper* steamhelper = &s_SteamHelper;
+CSteamClientHelper::CSteamClientHelper() = default;
+CSteamClientHelper::~CSteamClientHelper() = default;
+void CSteamClientHelper::Think() {}
+void CSteamClientHelper::SetAchievement(const char*) {}
+void CSteamClientHelper::SetStat(const char*, int) {}
+#else
 static CSteamAPIContext s_SteamAPIContext;
 CSteamAPIContext* steamapicontext = &s_SteamAPIContext;
 
@@ -77,4 +86,5 @@ void CSteamClientHelper::OnUserStatsStored(UserStatsStored_t* pCallback)
 void CSteamClientHelper::OnAchievementStored(UserAchievementStored_t* pCallback)
 {
 	// unused
-}
\ No newline at end of file
+}
+#endif
diff --git a/src/game/client/SteamClientHelper.h b/src/game/client/SteamClientHelper.h
index 923ed75..a091622 100644
--- a/src/game/client/SteamClientHelper.h
+++ b/src/game/client/SteamClientHelper.h
@@ -1,7 +1,9 @@
 #ifndef MS_STEAM_CLIENT_HELPER_H
 #define MS_STEAM_CLIENT_HELPER_H
 
+#ifndef MSR_STANDALONE
 #include <steam/steam_api.h>
+#endif
 
 class CSteamClientHelper
 {
@@ -14,13 +16,17 @@ public:
 	void SetStat(const char* str, int value);
 
 private:
+#ifndef MSR_STANDALONE
 	bool m_bHasLoadedSteamStats;
 	STEAM_CALLBACK(CSteamClientHelper, OnUserStatsReceived, UserStatsReceived_t, m_CallbackUserStatsReceived);
 	STEAM_CALLBACK(CSteamClientHelper, OnUserStatsStored, UserStatsStored_t, m_CallbackUserStatsStored);
 	STEAM_CALLBACK(CSteamClientHelper, OnAchievementStored, UserAchievementStored_t, m_CallbackAchievementStored);
+#endif
 };
 
+#ifndef MSR_STANDALONE
 extern CSteamAPIContext* steamapicontext;
+#endif
 extern CSteamClientHelper* steamhelper;
 
-#endif // MS_STEAM_HELPER_H
\ No newline at end of file
+#endif // MS_STEAM_HELPER_H
diff --git a/src/game/client/clientlibrary.cpp b/src/game/client/clientlibrary.cpp
index e074351..f348532 100644
--- a/src/game/client/clientlibrary.cpp
+++ b/src/game/client/clientlibrary.cpp
@@ -79,7 +79,7 @@ void CClientLibrary::HUDInit()
 	CVAR_CREATE("ms_hidehud", "0", FCVAR_ARCHIVE);						// Hides the HUD and viewmodel completely
 	CVAR_CREATE("ms_lildude", "1", FCVAR_ARCHIVE);						// Thothie MAR2007a - Hides the 3d Guy if set 0
 	CVAR_CREATE("ms_invtype", "1", FCVAR_ARCHIVE);						// MiB FEB2012_12 - Inventory types (added post-doc by Thothie)
-	CVAR_CREATE("ms_alpha_inventory", "0", FCVAR_ARCHIVE); // MiB FEB2019_24 [ALPHABETICAL_INVENTORY]
+	CVAR_CREATE("ms_alpha_inventory", "1", FCVAR_ARCHIVE); // Keep compact inventory entries sorted by name.
 	CVAR_CREATE("ms_doubleclicktime", "0.5", FCVAR_ARCHIVE);
 	CVAR_CREATE("ms_scrollamount", "30", FCVAR_ARCHIVE);
 
@@ -142,4 +142,4 @@ void CClientLibrary::RunFrame()
 	gRichPresence.Update();
 }
 
-extern CClientLibrary gClient;
\ No newline at end of file
+extern CClientLibrary gClient;
diff --git a/src/game/client/hud_msg.cpp b/src/game/client/hud_msg.cpp
index 00f6c5c..2a06a9e 100644
--- a/src/game/client/hud_msg.cpp
+++ b/src/game/client/hud_msg.cpp
@@ -25,6 +25,7 @@
 #include "clglobal.h"
 #include "clenv.h"
 #include "vgui_choosecharacter.h"
+#include "vgui_teamfortressviewport.h"
 #include "monsters/msmonster.h"
 #include "mslogger.h"
 #include "ms/angelscript/CAngelScriptManager.h"
@@ -53,6 +54,10 @@ constexpr unsigned int CLPERMENT_TOTALK = 3;
 
 void CHud ::MsgFunc_InitHUD(const char *pszName, int iSize, void *pbuf)
 {
+	// Reset map-specific UI here, never during a video-mode change.
+	if (gViewPort)
+		gViewPort->Initialize();
+
 	MS_INFO("[MsgFunc_InitHUD: EndMap]");
 	MSGlobals::EndMap(); //End old map
 
diff --git a/src/game/client/render/clrenderent.cpp b/src/game/client/render/clrenderent.cpp
index dd029cc..9f6f44d 100644
--- a/src/game/client/render/clrenderent.cpp
+++ b/src/game/client/render/clrenderent.cpp
@@ -339,7 +339,11 @@ void CRenderPlayerInset::Render()
 	if (!clplayer)
 		return;
 
-	//m_Ent.player = 0;
+	// This is a client-created preview entity with a reserved non-player index.
+	// The MSRDR_ASPLAYER path below supplies the real local player animation;
+	// leaving the engine player flag set makes Xash index player state with the
+	// reserved entity number while preparing bones for attached equipment.
+	m_Ent.player = 0;
 	m_Ent.model = clplayer->model;
 	m_Ent.index = MSGlobals::ClEntities[CLPERMENT_INSET];
 	m_Ent.curstate.number = MSGlobals::ClEntities[CLPERMENT_INSET];
diff --git a/src/game/client/render/studiomodelrenderer.cpp b/src/game/client/render/studiomodelrenderer.cpp
index ccd4067..c8cbc21 100644
--- a/src/game/client/render/studiomodelrenderer.cpp
+++ b/src/game/client/render/studiomodelrenderer.cpp
@@ -1329,6 +1329,8 @@ int CStudioModelRenderer::StudioDrawModel(int flags)
 
 			//Try to create an entity for this viewmodel (only lasts this frame)
 			cl_entity_t& RenderEnt = MSCLGlobals::CLViewEntities[hand];
+			// Xash validates the model when the entity is submitted.
+			RenderEnt.model = pModel;
 			int Ent = gEngfuncs.CL_CreateVisibleEntity(ET_NORMAL, &RenderEnt);
 			if (!Ent)
 				continue;
@@ -1336,7 +1338,6 @@ int CStudioModelRenderer::StudioDrawModel(int flags)
 			if (ViewModel_ExclusiveViewHand >= 0)
 				FoundExclusiveVModel = true;
 
-			RenderEnt.model = pModel;
 			studiohdr_t* pStudioHeader = (studiohdr_t*)IEngineStudio.Mod_Extradata(RenderEnt.model);
 
 			cl_entity_s* view = gEngfuncs.GetViewModel();
diff --git a/src/game/client/richpresence.cpp b/src/game/client/richpresence.cpp
index 677b71e..5785e60 100644
--- a/src/game/client/richpresence.cpp
+++ b/src/game/client/richpresence.cpp
@@ -24,7 +24,11 @@ void CRichPresence::Init(void)
 		m_bLoaded = true;
 		DiscordEventHandlers handlers;
 		memset(&handlers, 0, sizeof(handlers));
+#ifdef MSR_STANDALONE
+		Discord_Initialize("1063225093507010570", &handlers, 0, nullptr);
+#else
 		Discord_Initialize("1063225093507010570", &handlers, 1, "1961680");
+#endif
 		m_numStartTime = time(NULL);
 	}
 }
@@ -69,12 +73,14 @@ void CRichPresence::Update(void)
 		_snprintf(m_cBufferDetails, sizeof(m_cBufferDetails), "%s - %i HP", player.m_DisplayName.c_str(), currentHealth);
 		_snprintf(m_cBufferState, sizeof(m_cBufferState), "%s", MSGlobals::MapName.c_str());
 
+#ifndef MSR_STANDALONE
 		if (steamapicontext && steamapicontext->SteamFriends())
 		{
 			steamapicontext->SteamFriends()->SetRichPresence("name", player.m_DisplayName.c_str());
 			steamapicontext->SteamFriends()->SetRichPresence("zone", MSGlobals::MapName.c_str());
 			steamapicontext->SteamFriends()->SetRichPresence("steam_display", "#Status_InGame");
 		}
+#endif
 	}
 	else
 	{
@@ -82,8 +88,10 @@ void CRichPresence::Update(void)
 		strncpy(m_cBufferState, "Awaiting Greatness", sizeof(m_cBufferState));
 		clientsInGame = maxClients = 1;
 
+#ifndef MSR_STANDALONE
 		if (steamapicontext && steamapicontext->SteamFriends())
 			steamapicontext->SteamFriends()->SetRichPresence("steam_display", "#Status_MainMenu");
+#endif
 	}
 
 	DiscordRichPresence discordPresence;
@@ -95,4 +103,4 @@ void CRichPresence::Update(void)
 	discordPresence.partyMax = maxClients;
 	discordPresence.largeImageKey = "msr";
 	Discord_UpdatePresence(&discordPresence);
-}
\ No newline at end of file
+}
diff --git a/src/game/client/ui/ms/vgui_eventconsole.h b/src/game/client/ui/ms/vgui_eventconsole.h
index 2ce308e..d5aeec2 100644
--- a/src/game/client/ui/ms/vgui_eventconsole.h
+++ b/src/game/client/ui/ms/vgui_eventconsole.h
@@ -241,6 +241,16 @@ public:
 		Resize( );
 	}
 
+	void Layout(int x, int bottom, int width)
+	{
+		m_StartY = bottom;
+		setBounds(x, bottom, width, getTall());
+		const int lineWidth = V_max(1, (int)GetWidth() - (int)m_ScrollBarWidth);
+		for (unsigned int i = 0; i < V_min((int)EVENTCON_PREF_MAXLINES, EVENTCON_MAXLINES); ++i)
+			m_Line[i]->setSize(lineWidth, EVENTCON_LINE_SIZE_Y);
+		Resize();
+	}
+
 	void Resize( )
 	{
 		if( !m_VisibleLines || !ShowHUD())
@@ -347,4 +357,4 @@ public:
 		   *m_DecayTime, *m_BGTrans,
 		   *m_Width;
 	bool m_DynamicWidth;
-};
\ No newline at end of file
+};
diff --git a/src/game/client/ui/ms/vgui_health.h b/src/game/client/ui/ms/vgui_health.h
index eb23611..bb4fd84 100644
--- a/src/game/client/ui/ms/vgui_health.h
+++ b/src/game/client/ui/ms/vgui_health.h
@@ -51,6 +51,13 @@ namespace PrimaryHUD
 			m_Label = new MSLabel(this, "0/0", 0, getTall()/5, getWide(), YRES(8), MSLabel::a_center);
 		}
 
+		void Layout(int x, int y)
+		{
+			setBounds(x, y, BAR_W, BAR_H);
+			m_Image.setSize(getWide(), getTall());
+			m_Label->setBounds(0, getTall() / 5, getWide(), YRES(8));
+		}
+
 		void Update()
 		{
 			float Amt, MaxAmt;
@@ -192,6 +199,28 @@ namespace PrimaryHUD
 			}
 		}
 
+		void OnResolutionChanged() override
+		{
+			setBounds(0, 0, ScreenWidth(), ScreenHeight());
+			const int x = 10;
+			const int y = ScreenHeight() - 2 * BAR_H - YRES(10);
+			m_Bar[0]->Layout(x, y);
+			m_Bar[2]->Layout(x, y + BAR_H);
+			m_Bar[1]->Layout(x + BAR_W + EMBLEM_SIZE - 1, y);
+			m_Bar[3]->Layout(x + BAR_W + EMBLEM_SIZE - 1, y + BAR_H);
+			m_HUDImage.setSize(EMBLEM_SIZE, EMBLEM_SIZE);
+			m_HUDImage.setPos(x + BAR_W, y - 7 * BAR_SCALE);
+			CHARGE_W = XRES(30);
+			CHARGE_H = YRES(6);
+			CHARGE_SPACER_W = XRES(2);
+			for (int i = 0; i < 2; ++i)
+			{
+				const int chargeX = XRES(304) + (i == 0 ? -CHARGE_W - CHARGE_SPACER_W : CHARGE_SPACER_W);
+				m_Charge[i]->setBounds(chargeX, YRES(408), CHARGE_W, CHARGE_H);
+				m_ChargeLbl[i]->setBounds(chargeX, YRES(408), CHARGE_W, CHARGE_H);
+			}
+		}
+
 		//MiB NOV2007a - Moar Charge Colors!
 		void Update()
 		{
@@ -299,4 +328,4 @@ namespace PrimaryHUD
 			delete vHandsItems;
 		}
 	};
-}
\ No newline at end of file
+}
diff --git a/src/game/client/ui/ms/vgui_healthretro.h b/src/game/client/ui/ms/vgui_healthretro.h
index 7d5bb1e..c37bdea 100644
--- a/src/game/client/ui/ms/vgui_healthretro.h
+++ b/src/game/client/ui/ms/vgui_healthretro.h
@@ -46,6 +46,13 @@ namespace RetroHUD
 			m_Label = new MSLabel(this, "0/0", 0, getTall()/1.5, getWide(), YRES(8), MSLabel::a_center);
 		}
 
+		void Layout(int x, int y)
+		{
+			setBounds(x, y, FLASK_W, FLASK_H);
+			m_Image.setSize(getWide(), getTall());
+			m_Label->setBounds(0, getTall() / 1.5, getWide(), YRES(8));
+		}
+
 		void Update()
 		{
 			float Amt = !m_Type ? player.m_HP : player.m_MP;
@@ -129,6 +136,7 @@ namespace RetroHUD
 
 		//Weight ----------------------------
 		CStatusBar *m_pWeight;
+		MSLabel *m_StaminaLabel, *m_WeightLabel;
 		CStatusBar *m_Charge[2];
 		MSLabel *m_ChargeLbl[2];
 
@@ -179,6 +187,7 @@ namespace RetroHUD
 
 			MSLabel *pLabel = new MSLabel(m_pStamina, Localized("#STAMINA"), 0, (STAMINA_SIZE_Y / 2.0f) - (STAMINA_LBL_SIZE_Y / 2.0f), STAMINA_SIZE_X, STAMINA_LBL_SIZE_Y, MSLabel::a_center);
 			pLabel->SetFGColorRGB(Color_Text_White);
+			m_StaminaLabel = pLabel;
 
 			COLOR WeightColor(250, 150, 0, 100);
 
@@ -189,6 +198,7 @@ namespace RetroHUD
 			pLabel = new MSLabel(m_pWeight, Localized("#WEIGHT"), 0, (WEIGHT_SIZE_Y / 2.0f) - (WEIGHT_LBL_SIZE_Y / 2.0f), STAMINA_SIZE_X, WEIGHT_LBL_SIZE_Y, MSLabel::a_center);
 			//pLabel->setFgColor( 255, 255, 255, 64 );
 			pLabel->SetFGColorRGB(Color_Text_White);
+			m_WeightLabel = pLabel;
 
 			//Charge system
 			for (unsigned int i = 0; i < 2; i++)
@@ -204,6 +214,37 @@ namespace RetroHUD
 			}
 		}
 
+		void OnResolutionChanged() override
+		{
+			setBounds(0, 0, ScreenWidth(), ScreenHeight());
+			FLASK_SPACER = XRES(10);
+			FLASK_START_X = XRES(30);
+			FLASK_START_Y = ScreenHeight() - YRES(30) - FLASK_H;
+			MANA_FLASK_X = FLASK_START_X + FLASK_W + FLASK_SPACER;
+			m_Flask[0]->Layout(FLASK_START_X, FLASK_START_Y);
+			m_Flask[1]->Layout(MANA_FLASK_X, FLASK_START_Y);
+			STAMINA_X = FLASK_START_X;
+			STAMINA_Y = YRES(453);
+			STAMINA_SIZE_X = 2 * FLASK_W + FLASK_SPACER;
+			STAMINA_SIZE_Y = YRES(12);
+			STAMINA_LBL_SIZE_Y = YRES(10);
+			WEIGHT_SIZE_Y = YRES(10);
+			WEIGHT_LBL_SIZE_Y = WEIGHT_SIZE_Y;
+			m_pStamina->setBounds(STAMINA_X, STAMINA_Y, STAMINA_SIZE_X, STAMINA_SIZE_Y);
+			m_pWeight->setBounds(STAMINA_X, STAMINA_Y + STAMINA_SIZE_Y, STAMINA_SIZE_X, WEIGHT_SIZE_Y);
+			m_StaminaLabel->setBounds(0, (STAMINA_SIZE_Y - STAMINA_LBL_SIZE_Y) / 2, STAMINA_SIZE_X, STAMINA_LBL_SIZE_Y);
+			m_WeightLabel->setBounds(0, (WEIGHT_SIZE_Y - WEIGHT_LBL_SIZE_Y) / 2, STAMINA_SIZE_X, WEIGHT_LBL_SIZE_Y);
+			CHARGE_W = XRES(30);
+			CHARGE_H = YRES(6);
+			CHARGE_SPACER_W = XRES(2);
+			for (int i = 0; i < 2; ++i)
+			{
+				const int chargeX = XRES(320) + (i == 0 ? -CHARGE_W - CHARGE_SPACER_W : CHARGE_SPACER_W);
+				m_Charge[i]->setBounds(chargeX, STAMINA_Y, CHARGE_W, CHARGE_H);
+				m_ChargeLbl[i]->setBounds(chargeX, STAMINA_Y, CHARGE_W, CHARGE_H);
+			}
+		}
+
 		//MiB NOV2007a - Moar Charge Colors!
 		void Update()
 		{
@@ -326,4 +367,4 @@ namespace RetroHUD
 			delete vHandsItems;
 		}
 	};
-}
\ No newline at end of file
+}
diff --git a/src/game/client/ui/ms/vgui_hud.cpp b/src/game/client/ui/ms/vgui_hud.cpp
index 9d4df68..f939aa9 100644
--- a/src/game/client/ui/ms/vgui_hud.cpp
+++ b/src/game/client/ui/ms/vgui_hud.cpp
@@ -115,6 +115,7 @@ public:
 	virtual void Think(void);
 	//virtual void SetActiveInfo( int iInput );
 	virtual void Initialize(void);
+	void OnResolutionChanged();
 
 	void AddInfoWin(const char* Title, const char* Text);
 	void AddHelpWin(const char* Title, const char* Text);
@@ -151,6 +152,30 @@ private:
 
 VGUI_MainPanel* CreateHUDPanel(Panel* pParent) { return new CHUDPanel(pParent); }
 
+void HUD_ResolutionChanged()
+{
+	if (gViewPort && gViewPort->m_pHUDPanel)
+		static_cast<CHUDPanel*>(gViewPort->m_pHUDPanel)->OnResolutionChanged();
+}
+
+void CHUDPanel::OnResolutionChanged()
+{
+	// Recalculate from the current resolution, never from previously scaled pixels.
+	// Keep bars, chat history, selections and active status effects intact.
+	setBounds(0, 0, ScreenWidth(), ScreenHeight());
+	for (auto element : m_HUDElements)
+		element->OnResolutionChanged();
+	m_ID->setPos(ID_X, ID_Y);
+	m_DebugText->setPos(0, YRES(16));
+	m_StartSayText->setBounds(XRES(100), YRES(300), XRES(540), YRES(16));
+	m_StartSayText->m_Image.setPos(IMG_SPACER, (YRES(16) - IMG_SIZE) / 2);
+	m_StartSayText->m_TextPanel->setBounds(TEXT_START, 0, XRES(300) - TEXT_START - XRES(3), YRES(16));
+	m_Consoles[CON_EVENT]->Layout(XRES(390), ScreenHeight() - YRES(10), XRES(230));
+	m_Consoles[CON_SAYTEXT]->Layout(XRES(10), YRES(180), XRES(300));
+	for (auto window : m_HelpWindows)
+		window->setPos(ScreenWidth() - window->getWide() - XRES(60), YRES(10));
+}
+
 CHUDPanel::CHUDPanel(Panel* pParent) : VGUI_MainPanel(0, 0, 0, ScreenWidth(), ScreenHeight())
 {
 	setParent(pParent);
@@ -612,4 +637,4 @@ void HUD_DrawID(entinfo_t* pEntinfo)
 	if (!gViewPort || !gViewPort->m_pHUDPanel)
 		return;
 	((CHUDPanel*)gViewPort->m_pHUDPanel)->m_ID->Update(pEntinfo);
-}
\ No newline at end of file
+}
diff --git a/src/game/client/ui/ms/vgui_hud.h b/src/game/client/ui/ms/vgui_hud.h
index d016364..a85b127 100644
--- a/src/game/client/ui/ms/vgui_hud.h
+++ b/src/game/client/ui/ms/vgui_hud.h
@@ -21,6 +21,7 @@ void HUD_StartSayText(int Type);
 void HUD_CancelSayText();
 void HUD_DrawID(struct entinfo_t *pEntinfo);
 void HUD_NewLevel();
+void HUD_ResolutionChanged();
 bool QuickSlotConfirm();
 
-extern COLOR Color_Transparent, Color_Text_White;
\ No newline at end of file
+extern COLOR Color_Transparent, Color_Text_White;
diff --git a/src/game/client/ui/ms/vgui_ihud.h b/src/game/client/ui/ms/vgui_ihud.h
index 1e7bf0d..4f42511 100644
--- a/src/game/client/ui/ms/vgui_ihud.h
+++ b/src/game/client/ui/ms/vgui_ihud.h
@@ -4,9 +4,10 @@ class IHUD
 {
 public:
 	virtual void Init() {}
+	virtual void OnResolutionChanged() {} // Relayout without resetting live HUD state
 	virtual void Reset() {}									//New level
 	virtual void Update() {}								//Every frame
 	virtual void SetVisible(bool fVisible) {}				//Show/Hide
 	virtual void Close() {}									//Close
 	virtual IHUD *GetInterface() { return this; } //Get pointer to this interface
-};
\ No newline at end of file
+};
diff --git a/src/game/client/ui/ms/vgui_quickslot.h b/src/game/client/ui/ms/vgui_quickslot.h
index b1ab31a..261c2e7 100644
--- a/src/game/client/ui/ms/vgui_quickslot.h
+++ b/src/game/client/ui/ms/vgui_quickslot.h
@@ -66,6 +66,11 @@ public:
 		pGenericBolt->m_DisplayName = "Crude Wooden Bolt";
 	}
 
+	void OnResolutionChanged() override
+	{
+		setBounds(XRES(170), ScreenHeight() - QUICKSLOT_H, ScreenWidth() - XRES(170), QUICKSLOT_H);
+	}
+
 	void Update()
 	{
 		//Check if I've been holding a slot button for 2 seconds
@@ -373,4 +378,4 @@ public:
 		if (MSCLGlobals::DefaultHUDSounds.QuickSlot_Select)
 			PlayHUDSound(MSCLGlobals::DefaultHUDSounds.QuickSlot_Select, 1.0f);
 	}
-};
\ No newline at end of file
+};
diff --git a/src/game/client/ui/ms/vgui_vote.h b/src/game/client/ui/ms/vgui_vote.h
index b224b5a..cfd9a79 100644
--- a/src/game/client/ui/ms/vgui_vote.h
+++ b/src/game/client/ui/ms/vgui_vote.h
@@ -50,6 +50,15 @@ public:
 		m_YesVotes->setFgColor(255, 255, 255, 40);
 	}
 
+	void OnResolutionChanged() override
+	{
+		setBounds(0, 0, ScreenWidth(), ScreenHeight());
+		m_pVotePanel->setBounds(VOTE_START_X, VOTE_START_Y, VOTE_SIZE_X, VOTE_SIZE_Y);
+		m_pVoteTitle->setBounds(0, VOTE_SPACER_Y, VOTE_SIZE_X, VOTE_TITLE_SIZE_Y);
+		m_pVoteDesc->setBounds(VOTE_SPACER_X, VOTE_DESC_Y, VOTE_SIZE_X, VOTE_DESC_SIZE_Y);
+		m_YesVotes->setBounds(VOTE_SPACER_X, VOTE_INFAVOR_Y, VOTE_SIZE_X, YRES(480));
+	}
+
 	void Update()
 	{
 		if (MSGlobals::CurrentVote.fActive)
diff --git a/src/game/client/ui/vgui_int.cpp b/src/game/client/ui/vgui_int.cpp
index 00cac9f..39b3fae 100644
--- a/src/game/client/ui/vgui_int.cpp
+++ b/src/game/client/ui/vgui_int.cpp
@@ -20,6 +20,7 @@
 #include "vgui_controlconfigpanel.h"
 #include "clenv.h"
 #include "mslogger.h"
+#include "ms/vgui_hud.h"
 
 namespace
 {
@@ -95,13 +96,13 @@ void VGui_Startup()
 
 	if (gViewPort != NULL)
 	{
-		//		root->removeChild(gViewPort);
-
-		// free the memory
-		//		delete gViewPort;
-		//		gViewPort = NULL;
-
-		gViewPort->Initialize();
+		// VidInit also runs when changing fullscreen mode or resolution.
+		// Keep the current menus and HUD flags; InitHUD resets them on map entry.
+		gViewPort->setParent(root);
+		gViewPort->setBounds(0, 0, root->getWide(), root->getTall());
+		gViewPort->UpdateCursorState();
+		MS_INFO("[VGui_Startup: Preserved UI, hideHUD=%d, viewport=%dx%d]",
+			gHUD.m_iHideHUDDisplay, root->getWide(), root->getTall());
 	}
 	else
 	{
@@ -110,6 +111,8 @@ void VGui_Startup()
 		gViewPort->Initialize(); //Master Sword - call Initialize the first time the viewport is created
 	}
 
+	HUD_ResolutionChanged();
+	MS_INFO("[VGui_Startup: HUD layout refreshed for %dx%d]", ScreenWidth(), ScreenHeight());
 	MS_INFO("[VGui_Startup: Complete]");
 
 	/*
diff --git a/src/game/client/ui/vgui_status.h b/src/game/client/ui/vgui_status.h
index ee4f618..44ebcf6 100644
--- a/src/game/client/ui/vgui_status.h
+++ b/src/game/client/ui/vgui_status.h
@@ -203,6 +203,13 @@ public:
 		m_FN.setVisible(false);
 	}
 
+	void OnResolutionChanged() override
+	{
+		setBounds(0, 0, ScreenWidth(), ScreenHeight());
+		m_FN.setSize(FN_W, FN_H);
+		m_FN.setPos(ScreenWidth() - FN_W, 10);
+	}
+
 	void Update(void)
 	{
 		//Update/Check Status Icons
@@ -413,4 +420,4 @@ int __MsgFunc_StatusIcons(const char *pszName, int iSize, void *pbuf)
 		SetFN(false);
 
 	return 0;
-}
\ No newline at end of file
+}
diff --git a/src/game/server/client.cpp b/src/game/server/client.cpp
index f14b662..937cdf3 100644
--- a/src/game/server/client.cpp
+++ b/src/game/server/client.cpp
@@ -46,6 +46,10 @@
 #include "ms/angelscript/CAngelScriptManager.h"
 #include "ms/angelscript/ASModuleSystem.h"
 #include "mslogger.h"
+#ifdef MSR_STANDALONE
+#include "fn/StandaloneIdentity.h"
+static ID64 g_PrivateFNIds[32] = {};
+#endif
 
 extern void PlayerPrecache();
 
@@ -90,6 +94,22 @@ called when a player connects to a server
 */
 BOOL ClientConnect(edict_t *pEntity, const char *pszName, const char *pszAddress, char szRejectReason[128])
 {
+#ifdef MSR_STANDALONE
+	const ID64 privateId = StandaloneFN::AccountId(g_engfuncs.pfnInfoKeyValue(g_engfuncs.pfnGetInfoKeyBuffer(pEntity), "_fnid"));
+	if (!privateId)
+	{
+		strcpy(szRejectReason, "Start the game with Play-Local.cmd to load your private FN profile.");
+		return 0;
+	}
+	for (int i = 0; i < 32; ++i)
+	{
+		if (i != ENTINDEX(pEntity) - 1 && g_PrivateFNIds[i] == privateId)
+		{
+			strcpy(szRejectReason, "This private FN profile is already connected. Use a separate player profile.");
+			return 0;
+		}
+	}
+#endif
 	bool fSuccess = false;
 
 	MS_INFO("[ClientConnect]	");
@@ -100,6 +120,9 @@ BOOL ClientConnect(edict_t *pEntity, const char *pszName, const char *pszAddress
 	{
 		// this has to be here otherwise on reconnect players can't select characters
 		int iPlayerOfs = ENTINDEX(pEntity) - 1;
+#ifdef MSR_STANDALONE
+		g_PrivateFNIds[iPlayerOfs] = privateId;
+#endif
 		clientaddr_t &ClientInfo = g_NewClients[iPlayerOfs];
 		ClientInfo.pe = pEntity;
 		ClientInfo.TimeClientConnected = gpGlobals->time;
@@ -160,6 +183,10 @@ GLOBALS ASSUMED SET:  g_fGameOver
 
 void ClientDisconnect(edict_t *pEntity)
 {
+#ifdef MSR_STANDALONE
+	if (pEntity && ENTINDEX(pEntity) >= 1 && ENTINDEX(pEntity) <= 32)
+		g_PrivateFNIds[ENTINDEX(pEntity) - 1] = 0;
+#endif
 	if (g_fGameOver)
 		return;
 
@@ -277,7 +304,11 @@ void ClientPutInServer(edict_t *pEntity)
 	MS_INFO("Connecting %s (%s)", pPlayer->DisplayName(), pPlayer->m_ClientAddress);
 
 	// Read Profile from FN, if possible.
+#ifdef MSR_STANDALONE
+	pPlayer->steamID64 = g_PrivateFNIds[ENTINDEX(pEntity) - 1];
+#else
 	pPlayer->steamID64 = UTIL_ComputeSteamID64(GETPLAYERAUTHID(pEntity));
+#endif
 
 	// Allocate a CBasePlayer for pev, and call spawn
 	pPlayer->Spawn();
diff --git a/src/game/shared/ms/msmonstershared.cpp b/src/game/shared/ms/msmonstershared.cpp
index 40c837d..f7f7fb3 100644
--- a/src/game/shared/ms/msmonstershared.cpp
+++ b/src/game/shared/ms/msmonstershared.cpp
@@ -622,7 +622,8 @@ void CMSMonster ::MarkDamage(CBasePlayer * pPlayer, int vStat, int vProp, float
 	if (!FStrEq(vsId, vPlayerDamage.msId))
 	{
 		vPlayerDamage.Clear();
-		strncpy(vPlayerDamage.msId, vsId, 32);
+		strncpy(vPlayerDamage.msId, vsId, sizeof(vPlayerDamage.msId) - 1);
+		vPlayerDamage.msId[sizeof(vPlayerDamage.msId) - 1] = 0;
 	}
 	
 	vPlayerDamage.dmgInTotal += vAmount;
diff --git a/src/game/shared/stats/statdefs.h b/src/game/shared/stats/statdefs.h
index 1f4a4d1..b639212 100644
--- a/src/game/shared/stats/statdefs.h
+++ b/src/game/shared/stats/statdefs.h
@@ -81,7 +81,9 @@ struct playerdamage_t
 {
 	float dmg[SKILL_MAX_ATTACK][STAT_PROP_ALL_TOTAL]; //Record damage done with with each combat skill
 	float dmgInTotal; //MiB Mar2008a - For keeping track of all damage a player has done, regardless of what skill it was
-	char msId[32]; // MiB MAR2019_22 [SLOT_EXP] - Keep track of the steam id / slot to avoid passing exp to the wrong character
+	// Xash Internet clients use IDs such as "ID_<32-character hash>". Keep
+	// enough room for that ID, the character-slot suffix, and the terminator.
+	char msId[64]; // Keep track of the authenticated player / character slot for XP ownership.
 
 	// MiB MAR2019_22 [SLOT_EXP] - Added because this is done in multiple places and shouldn't be written out...
 	void Clear()
diff --git a/src/game/server/fn/StandaloneIdentity.h b/src/game/server/fn/StandaloneIdentity.h
new file mode 100644
--- /dev/null
+++ b/src/game/server/fn/StandaloneIdentity.h
@@ -0,0 +1,22 @@
+#pragma once
+#include <cstdint>
+#include <cstring>
+
+namespace StandaloneFN
+{
+// A launcher-generated 128-bit profile key is portable between private hosts.
+// Retain FN's decimal uint64 wire field; the high bit separates private IDs
+// from Steam's public individual-account range. No Steam identity is asserted.
+inline std::uint64_t AccountId(const char* profile)
+{
+    if (!profile || std::strlen(profile) != 32) return 0;
+    std::uint64_t hash = 14695981039346656037ULL;
+    for (int i = 0; i < 32; ++i)
+    {
+        const unsigned char c = static_cast<unsigned char>(profile[i]);
+        if (!((c >= '0' && c <= '9') || (c >= 'a' && c <= 'f'))) return 0;
+        hash = (hash ^ c) * 1099511628211ULL;
+    }
+    return hash | 0x8000000000000000ULL;
+}
+}
